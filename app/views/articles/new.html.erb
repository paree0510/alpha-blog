<h1>Create a new article</h1>

<% if @article.errors.any? %>
<h4>Following errors led to non-creation of new article: </h4>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li>
        <%= msg %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render 'form' %>